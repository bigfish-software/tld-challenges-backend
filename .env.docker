# Docker Environment Configuration for TLD Challenges Backend
# Copy this file to .env and update the values for your environment

# Node Environment
NODE_ENV=production

# Database Configuration (connects to tld-challenges-db container)
DATABASE_HOST=tld-challenges-db
DATABASE_PORT=5432
DATABASE_NAME=tld_challenges
DATABASE_USERNAME=cms
DATABASE_PASSWORD=your_secure_database_password_here
DATABASE_SSL=false

# Strapi Security Keys - CHANGE THESE IN PRODUCTION!
# Generate new keys using: node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
APP_KEYS="app-key-1,app-key-2,app-key-3,app-key-4"
API_TOKEN_SALT=your_api_token_salt_here
ADMIN_JWT_SECRET=your_admin_jwt_secret_here
TRANSFER_TOKEN_SALT=your_transfer_token_salt_here
JWT_SECRET=your_jwt_secret_here
ENCRYPTION_KEY=your_encryption_key_here

# Frontend API Token (Long-lived JWT for frontend application)
FRONTEND_API_TOKEN=your_frontend_api_token_here
